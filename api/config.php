<?php
/**
 * Реквизиты MySQL на хостинге reg.ru.
 *
 * Заполните значения из панели reg.ru → «Базы данных» (созданная БД и её
 * пользователь). Файл коммитится и попадает на сервер при каждом деплое.
 * Держите репозиторий приватным.
 *
 * ВАЖНО ПРО СПЕЦСИМВОЛЫ В ПАРОЛЕ:
 *   Значения заключены в ОДИНАРНЫЕ кавычки. Внутри них «магические» только два
 *   символа: одинарная кавычка ' и обратный слеш \.
 *   - Если пароль содержит '  → замените её на \'   (пример: 'pa\'ss')
 *   - Если пароль содержит \  → удвойте его: \\     (пример: 'pa\\ss')
 *   - Символы $ " и прочие в одинарных кавычках НЕ экранируются — их пишите как есть.
 *   Если пароль сложный и постоянно ломается — проще сменить его в панели reg.ru
 *   на буквенно-цифровой (без ' и \) и вписать заново.
 *
 * Прямой доступ к этому файлу по URL запрещён (см. api/.htaccess).
 */
return [
    'db_host'     => 'localhost',          // на reg.ru почти всегда localhos
    'db_name'     => 'u3617849_default',               // имя созданной базы данных
    'db_user'     => 'u3617849_default',          // пользователь базы
    'db_pass'     => '1S5Nrmf2tpvU5rlE',          // пароль пользователя базы
main
    'session_ttl' => 7 * 24 * 3600,        // время жизни сессии, сек (7 дней)
];
